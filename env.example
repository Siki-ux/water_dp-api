# Water Data Platform Environment Configuration
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root: cp env.example .env
# 2. Update the values below with your actual configuration
# 3. Never commit .env to version control (it's in .gitignore)
#
# All configuration values are read from environment variables.
# This file serves as a template with example values.

# Application
APP_NAME=Water Data Platform
DEBUG=false
VERSION=1.0.0
API_PREFIX=/api/v1
CORS_ORIGINS=["*"]

# Database
# PostgreSQL connection string format: postgresql://[user]:[password]@[host]:[port]/[database]
# 
# For local development (connecting to local PostgreSQL):
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/water_data
#
# For Docker Compose (connecting to postgres service):
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/water_data
#
# For production (with SSL):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require
#
# IMPORTANT: Replace 'your_password' with your actual PostgreSQL password!
POSTGRES_DB=water_app
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/water_app
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# Redis
REDIS_URL=redis://localhost:6379
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# GeoServer
GEOSERVER_URL=http://localhost:8080/geoserver
GEOSERVER_USERNAME=admin
GEOSERVER_PASSWORD=geoserver
GEOSERVER_WORKSPACE=water_data

# Time Data Processing
TIME_ZONE=UTC
MAX_TIME_RANGE_DAYS=365
FROST_URL=http://localhost:8083/FROST-Server/v1.1
KEYCLOAK_URL=http://localhost:8081

# Security
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Monitoring
SENTRY_DSN=
LOG_LEVEL=INFO
